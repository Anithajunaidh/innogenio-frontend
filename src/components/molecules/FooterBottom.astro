---
import ThemeToggle from "../atoms/ThemeToggle.astro";

interface Props {
  year?: number;
}
const { year = new Date().getFullYear() } = Astro.props;
---

<div class="flex justify-between items-center">
  <p class="text-sm text-foreground/50">Mistral AI © {year}</p>
  <ThemeToggle />
</div>
