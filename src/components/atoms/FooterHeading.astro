---
/* Footer column heading (innogenio-home-v2 .f-col h5) — mono, uppercase,
   muted. */
interface Props {
	text: string;
}
const { text } = Astro.props;
---

<h3 class="font-mono text-f-head text-ink-secondary uppercase">{text}</h3>
